function solution(priorities, location) {
  const arr = priorities.map((val, idx) => {
    return { val, idx };
  });
  while (arr.length) {
    const q = array.shift();
  }

  var answer = 0;
  return answer;
}

solution([2, 1, 3, 2]);
