const s = "aukks";
const skip = "wbqd";
let answer = "";
const str = new RegExp(`[^${skip}]`, "g");
const alpha = "abcdefghijklmnopqrstuvwxyz".match(); //

for (let c of s) {
  const index = alpha.indexOf(c) + index;
}
